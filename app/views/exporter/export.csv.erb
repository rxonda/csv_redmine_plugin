<% @timeEntries.each do |t| %><%= CSV.generate_line([t[:centroCusto], t[:matricula], t[:cargo], t[:codigoSAP], ('%.2f' % t[:qtd]).gsub(/\./,','), ('%.2f' % t[:horaExtra]).gsub(/\./,','), t[:objetoCusto]], {:row_sep => "\r\n",:col_sep => ';'}) %><% end %>